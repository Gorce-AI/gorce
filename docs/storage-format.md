# Storage format (deferred)

S1 has no storage format and no durable runtime. This document remains only as
a boundary marker for a later approved phase.

No layout, encoding, atomic-write, index, migration, or compatibility
invariant is active in S1.
