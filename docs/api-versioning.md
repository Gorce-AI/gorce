# API versioning

S1 retires the placeholder `api/` tree and defines no product API, transport,
health endpoint, or storage format. API versioning is deferred until a
successor phase explicitly introduces runtime contracts.

## Rules

No product API compatibility, versioning, request identifiers, or schema
publication claim is made by S1. The JSON evidence schemas under `tests/qa/`
are internal verification contracts, not product APIs.
